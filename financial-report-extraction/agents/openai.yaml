interface:
  display_name: "财报提取"
  short_description: "批量图片/PDF OCR、勾稽校验、标准化并输出标准表"
  default_prompt: "Use $financial-report-extraction to OCR uploaded financial-report images or PDFs, group and order multiple files into statement units when needed, confirm groups with the user, map each statement vlm_text into JSON, validate it, standardize it, and output standard-table Excel files."

policy:
  allow_implicit_invocation: true
